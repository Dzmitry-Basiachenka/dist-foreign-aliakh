<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="IFundPoolMapper">

    <resultMap id="preServiceFeeFundResultMap" type="PreServiceFeeFund" extends="StoredEntityMapper.storedEntityResult">
        <result property="id" column="df_fund_pool_uid"/>
        <result property="name" column="name"/>
        <result property="amount" column="withdrawn_amount"/>
        <result property="comment" column="comment"/>
    </resultMap>

    <sql id="preServiceFeeFundColumns">
        df_fund_pool_uid,
        name,
        withdrawn_amount,
        comment,
        <include refid="StoredEntityMapper.additionalColumns"/>
    </sql>

    <insert id="insert" parameterType="PreServiceFeeFund">
        insert into ${schema}.df_fund_pool (
            <include refid="preServiceFeeFundColumns"/>
        ) values (
            #{id},
            #{name},
            #{amount},
            #{comment},
            <include refid="StoredEntityMapper.insert"/>
        )
    </insert>

    <select id="findById" resultMap="preServiceFeeFundResultMap" parameterType="string">
        select
            <include refid="preServiceFeeFundColumns"/>
        from ${schema}.df_fund_pool
        where df_fund_pool_uid = #{fundPoolId}
    </select>

    <select id="findAll" resultMap="preServiceFeeFundResultMap">
        select
            <include refid="preServiceFeeFundColumns"/>
        from ${schema}.df_fund_pool
        order by updated_datetime desc
    </select>

    <delete id="delete" parameterType="string">
        delete from ${schema}.df_fund_pool
        where df_fund_pool_uid = #{fundPoolId}
    </delete>

    <select id="findNamesByUsageBatchId" resultType="string" parameterType="string">
        select distinct name
        from ${schema}.df_fund_pool
        where df_fund_pool_uid in (
            select ufas.df_fund_pool_uid
            from ${schema}.df_usage u
            join ${schema}.df_usage_fas ufas on u.df_usage_uid = ufas.df_usage_fas_uid
            where df_usage_batch_uid = #{df_usage_batch_id}
            union
            select ufas.df_fund_pool_uid
            from ${schema}.df_usage_archive u
            join ${schema}.df_usage_fas ufas on u.df_usage_archive_uid = ufas.df_usage_fas_uid
            where df_usage_batch_uid = #{df_usage_batch_id}
        )
    </select>

    <select id="findNotAttachedToScenario" resultMap="preServiceFeeFundResultMap">
        select
            fund.df_fund_pool_uid,
            fund.name
        from ${schema}.df_fund_pool fund
        where df_fund_pool_uid not in (
            select nts_fields->>'pre_service_fee_fund_uid'
            from ${schema}.df_scenario
            where nts_fields->>'pre_service_fee_fund_uid' is not null
        )
        order by fund.updated_datetime desc
    </select>

    <select id="findCountByName" parameterType="string" resultType="int">
        select count(name)
        from ${schema}.df_fund_pool
        where upper(name) = upper(#{name})
    </select>
</mapper>
